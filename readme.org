#+title: BYN Manual LaTeX Conversion
#+date: 2026-09-06
#+language: en
#+startup: overview

* Goal

Convert the BYN radiation-safety manual into a maintainable, reproducible LaTeX book project.

The finished project should have:

- one canonical LaTeX entry point;
- clean chapter/source files instead of page-by-page OCR fragments;
- source PDF/OCR material separated from generated and scratch files;
- figures stored in a predictable place;
- reproducible build commands;
- no credentials, editor cache, or LaTeX build products committed as source.

See [[file:todo.org][todo.org]] for the working plan and page-audit tracker.

* Current state

This repository is mid-conversion and contains original source material, generated OCR fragments, reviewed front matter, figures, and build output in their separate working directories.

Important current files:

| Path | Purpose | Notes |
|------+---------+-------|
| [[file:source/byn.pdf][source/byn.pdf]] | Original BYN manual PDF | Canonical visual source. |
| [[file:source/byn.txt][source/byn.txt]] | Text extraction/OCR from the PDF | Useful reference, but has OCR errors. |
| [[file:source/pages/][source/pages/]] | Page images =img-001.png= through =img-116.png= | Current page-image archive. |
| [[file:scratch/ocr-input/][scratch/ocr-input/]] | OCR input queue | Currently empty. |
| [[file:scratch/generated-pages/][scratch/generated-pages/]] | Raw generated page LaTeX fragments | =img-001.png.tex= through =img-116.png.tex=; not final source. |
| [[file:tex/main.tex][tex/main.tex]] | Canonical LaTeX entry point | Inputs reviewed front matter, then provisional generated fragments for pages 009--116. |
| [[file:tex/preamble.tex][tex/preamble.tex]] | Shared LaTeX preamble | Packages, graphics path, colors, and document-wide settings. |
| [[file:tex/frontmatter/objectives.tex][tex/frontmatter/objectives.tex]] | Reviewed course objective | Transcription of source page 001. |
| [[file:tex/frontmatter/program.tex][tex/frontmatter/program.tex]] | Reviewed course program | Transcription of source pages 003--008; source page 002 is blank. |
| [[file:tex/chapters/][tex/chapters/]] | Reviewed chapter files | Pages 009--018 are migrated here; page 014 still has a flagged source-text ambiguity. |
| [[file:assets/figures/][assets/figures/]] | Manual figures used by the LaTeX drafts | Root-level figure PNG/SVG assets have been moved here. |
| [[file:scripts/ocr_pages_openai.py][scripts/ocr_pages_openai.py]] | Sanitized OCR/AI helper script | Uses project-relative paths and reads =OPENAI_API_KEY= from the environment. Generated text still requires human review. |
| [[file:flake.nix][flake.nix]] | Nix development shell | Provides TeX Live full scheme. |
| =*.aux=, =*.log=, =*.fls=, =*.fdb_latexmk=, =*.synctex.gz=, =.auctex-auto/= | LaTeX/editor generated files | Build/cache artifacts; should be ignored or removed. |

* Quick start

If Nix is available:

#+begin_src sh
nix develop
#+end_src

Current canonical build:

#+begin_src sh
make pdf
# writes build/main.pdf from tex/main.tex
#+end_src

Other useful targets:

#+begin_src sh
make pdf
make clean
make cleanall
#+end_src

The same build is available directly through =latexmk=; project options live in [[file:latexmkrc][latexmkrc]]. All LaTeX auxiliary/build products are confined to =build/=.

#+begin_src sh
latexmk tex/main.tex
#+end_src

* Recommended target layout

Refactor toward this structure:

#+begin_example
.
├── readme.org
├── todo.org
├── flake.nix
├── flake.lock
├── tex/
│   ├── main.tex
│   ├── preamble.tex
│   ├── frontmatter/
│   ├── chapters/
│   └── appendices/
├── assets/
│   └── figures/
├── source/
│   ├── byn.pdf
│   ├── byn.txt
│   └── pages/
├── scratch/
│   └── generated-pages/
├── scripts/
└── build/
#+end_example

Suggested meanings:

| Directory | Contents |
|-----------+----------|
| =tex/= | Final, reviewed LaTeX source. |
| =assets/figures/= | Figures included by final LaTeX. |
| =source/= | Original PDF, OCR text, and page images. Do not edit in place. |
| =scratch/= | Raw AI/OCR/generated fragments and experiments. Can be regenerated. |
| =scripts/= | Sanitized conversion/helper scripts. |
| =build/= | Generated PDFs and LaTeX intermediate files. Do not commit. |

* Conversion workflow

For each page or section:

1. Open the source page image/PDF and the generated fragment.
2. Compare against =source/byn.txt= only as a secondary reference.
3. Correct OCR errors, spelling, accents, math, units, and punctuation.
4. Remove AI hallucinations or invented wording.
5. Convert page-local markup into semantic LaTeX: =\chapter=, =\section=, tables, figures, lists, equations.
6. Move reviewed content into the appropriate chapter file under =tex/chapters/=.
7. Build with =latexmk= and fix errors/warnings introduced by the change.
8. Mark the page/range as reviewed in [[file:todo.org][todo.org]]. Pages 001 and 003--008 are integrated under [[file:tex/frontmatter/][tex/frontmatter/]]; page 002 is blank in the source scan.

* LaTeX conventions

- Keep all source files UTF-8.
- Prefer real Spanish accents over TeX accent commands unless a package requires otherwise.
- Use one shared preamble in =tex/preamble.tex=.
- Add =\graphicspath{{assets/figures/}}= after moving figures.
- Use semantic structure rather than formatting-only commands where possible.
- Keep page-fragment files out of the final source tree unless they have been reviewed and integrated.
- Build with =-halt-on-error= once the current rough draft is clean enough.

* Known cleanup/issues

- [[file:tex/main.tex][tex/main.tex]] now exists as the canonical build entry point. Reviewed front matter and pages 009--018 are integrated; pages 019--116 still temporarily come from raw generated fragments.
- The generated page fragments in =scratch/generated-pages/= for pages 019--116 need human review and migration into semantic chapter files. Page 014 remains flagged for a source-text clarification.
- The previous malformed list/enumerate blocker around =scratch/generated-pages/img-073.png.tex= is fixed enough for strict builds, but the page still needs source-image review.
- Some generated fragments likely contain OCR/AI mistakes, English phrases, or hallucinated headings.
- Legacy drafts and old helper tools were archived and have now been deleted from the working tree; use Git history to restore if necessary.
- A =.gitignore= now covers LaTeX, editor, and build artifacts; generated intermediates have been cleaned from the working tree.
- The OCR helper has been sanitized, but should be treated as an optional regeneration tool and not as reviewed source.

* Publication note

Before publishing or distributing the converted manual, verify ownership/licensing of the original BYN PDF and any extracted figures.
